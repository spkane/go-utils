package debugtools

import "testing"

/* func TestCheckError(t *testing.T) {
  var tests = []struct {
    s, p, want string
  }{
    {"YippeYahoo", "Yahoo", "Yippe"},
    {"1,2,3,\n", ",\n", "1,2,3"},
  }
  for _, c := range tests {
    got := TrimSuffix(c.s, c.p)
    if got != c.want {
      t.Errorf("TrimSuffix(%q, %q) == %q, want %q", c.s, c.p, got, c.want)
    }
  }
} */
